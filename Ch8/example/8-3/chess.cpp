/**
棋盘左上位置 (tr, tc)
黑色位置 (dr, dc)
size - 棋盘边长 */
int chessBoard(int tr, int tc, int dr, int dc, int size) {
  if (size == 1) {
    return 0;
  }
  int s = size / 2;
  //   判断四个象限
  //   如果左上
  //   chessBoard(tr, tc, dr, dc, s);
  //   chessBoard(tr, tc+s, dr+s-1, dc+s, s);
  //   chessBoard
  //   chessBoard
  //   剩下 3 个象限
  //
  return 0;
}